---
name: prompt-clarify
description: Analyzes and improves AI prompts through iterative questioning. Use this skill whenever a user wants to refine or improve a prompt — even if they just paste a rough draft without explicitly asking for help. Also trigger when the user says their AI results are not matching expectations, when they ask "how do I phrase this?", or when they share a task description and seem unsure how to turn it into a prompt.
---

# prompt-clarify

사용자가 작성한 AI 지시 프롬프트를 분석하여, 모호하거나 불충분한 부분을 찾아내고 역질문을 통해 프롬프트를 구체화·개선해주는 스킬.

## 사용법

```
/prompt-clarify
```

실행 후 분석할 프롬프트를 붙여넣으면 된다.

## 동작 방식

### 1단계: 프롬프트 입력 받기

사용자에게 개선하고 싶은 프롬프트를 입력해달라고 요청한다.

### 2단계: 명확성 분석 (최대 3라운드)

입력된 프롬프트를 아래 기준으로 분석한다. 각 항목은 AI가 결과물을 제대로 생성하는 데 필요한 정보가 충분한지를 확인하기 위한 것이다.

**분석 항목**
- **목적/목표**: AI에게 기대하는 결과가 명확한가
- **맥락/배경**: 필요한 배경 정보가 충분히 제공되었는가
- **출력 형식**: 결과물의 형식, 길이, 구조가 명시되었는가
- **대상/범위**: 작업의 범위와 제약 조건이 분명한가
- **기준/조건**: 성공 기준이나 품질 조건이 포함되어 있는가
- **예시**: 예시나 참고 자료가 있으면 도움이 되는 상황인가
- **작업 복잡도**: 이 작업이 단일 AI로 처리 가능한가, 아니면 멀티에이전트 팀이 필요한가
  - 단일 AI 적합: 단일 결과물, 단순 변환, 짧은 컨텍스트
  - 멀티에이전트 적합: 아래 조건 중 2개 이상 해당되는 경우
    - 결과물이 3개 이상의 독립적인 산출물로 구성됨
    - 각 단계가 서로 다른 전문 역할을 요구함 (예: 조사 + 작성 + 검수)
    - 단계 간 병렬 처리 시 시간 효율이 명확히 향상됨
  - 멀티에이전트가 적합하다고 판단되면 내부 플래그를 세워 3단계에서 구성도 제안으로 이어지도록 한다

**라운드 진행 규칙**
- 모호하거나 누락된 항목에 대해서만 질문을 생성한다 (이미 명확한 항목은 질문하지 않는다)
- 질문 수는 실제 필요한 개수만큼만 (과도한 질문 금지)
- 사용자가 답변하면 재분석하여 여전히 불명확한 부분이 있으면 다음 라운드 진행
- 아래 조기 종료 조건을 모두 충족하면 라운드를 조기 종료한다:
  - 목적/목표, 출력 형식, 대상/범위가 모두 명확히 정의됨
  - 추가 질문이 결과물 품질을 의미 있게 개선하지 않을 것으로 판단됨
- 최대 3라운드 후 반드시 개선된 프롬프트를 출력

**엣지 케이스 처리**
- **프롬프트가 이미 충분히 명확한 경우**: 질문 없이 바로 3단계로 이동하며, "이미 잘 작성된 프롬프트입니다"라고 안내한 뒤 소폭 개선된 버전을 출력한다
- **사용자가 질문에 답하지 않거나 "모르겠다"고 하는 경우**: 해당 항목은 빈칸으로 두고, 프롬프트에 "※ [항목명] 미정 — AI가 적절히 판단" 주석을 추가하여 출력한다
- **프롬프트가 너무 짧아 분석 자체가 불가능한 경우**: 먼저 어떤 작업을 원하는지 한 문장으로 설명해달라고 요청한다

**각 라운드 출력 형식**
```
[라운드 N/3] 다음 사항을 확인하고 싶습니다:

1. (질문)
2. (질문)
...
```

### 3단계: 개선된 프롬프트 출력

모든 라운드가 끝나면 수집된 정보를 바탕으로 개선된 프롬프트를 출력한다.

**출력 형식**
```
## 개선된 프롬프트

(개선된 프롬프트 전문)

---
## 주요 개선 사항
- (원본 대비 달라진 점 목록)

---
## [조건부] 에이전트 팀 구성 제안
> 작업 복잡도 분석에서 멀티에이전트가 적합하다고 판단된 경우에만 출력한다.

| 에이전트 | 역할 | 입력 | 출력 |
|----------|------|------|------|
| 조사 에이전트 | 관련 자료 수집 및 요약 | 사용자 키워드 | 리서치 요약본 |
| 작성 에이전트 | 본문 초안 작성 | 리서치 요약본 | 초안 문서 |
| 검수 에이전트 | 품질 기준 검토 및 피드백 | 초안 문서 | 최종 결과물 |

흐름: 조사 에이전트 → 작성 에이전트 → 검수 에이전트 → 최종 결과물

---
## 스킬 추천 및 갭 검토

### 권장 스킬
> 스킬 추천 절차:
> 1. 아래 7개 레이어, 총 85개 스킬을 대상으로 스캔한다:
>    - Project-Local (`.claude/skills/`, 9개)
>    - bkit Core v1.6.1 (`~/.claude/plugins/cache/bkit-marketplace/bkit/1.6.1/skills/`, 31개)
>    - superpowers v5.0.6 (`~/.claude/plugins/cache/superpowers-dev/superpowers/5.0.6/skills/`, 14개)
>    - Anthropic 공식 (`~/.claude/skills/`, 10개)
>    - Design 플러그인 (7개), team-attention-plugins (4개), CC Built-in (10개)
> 2. 각 스킬에 대해 아래 두 기준 중 하나 이상을 만족하면 권장으로 분류한다:
>    - 프롬프트의 작업 유형과 스킬의 목적이 연관되는 경우
>    - 이 스킬의 결과물이 다음 단계로 자연스럽게 이어지는 경우
>      (예: 명확화 완료 → eval-rubric으로 평가 기준 생성)

| 스킬 | 추천 이유 |
|------|-----------|
| (스킬명) | (이유) |

### 갭 스킬 (보유하지 않은 스킬)
> 위 검토 과정에서 필요하다고 판단되었으나 현재 보유하지 않은 스킬을 여기 기록한다.
> 없으면 이 섹션을 생략한다.

| 필요 스킬 | 예상 역할 | 필요 이유 |
|-----------|-----------|-----------|
| (스킬명)  | (역할)    | (이유)    |
```

**에이전트 구성 자기 검토 (구성도 출력 전 내부 수행)**

구성도를 출력하기 전에 아래 항목을 스킬 스스로 검토하고, 문제가 있으면 구성을 수정한 후 출력한다:

- 역할 분리: 각 에이전트의 책임 범위가 명확히 구분되며 중복되지 않는가
- 의존 관계: 에이전트 간 입출력 흐름이 논리적 순서를 따르는가
- 중복 통합 가능성: 단일 에이전트로 합쳐도 효율에 손실이 없는 역할이 분리되어 있지 않은가

## 예시 시나리오

**원본 프롬프트 (나쁜 예)**
> "마케팅 글 써줘"

**1라운드 질문 예시**
1. 어떤 제품/서비스에 대한 마케팅 글인가요?
2. 타깃 독자는 누구인가요? (연령대, 직군 등)
3. 글의 목적은 무엇인가요? (구매 유도, 브랜드 인지도, 이벤트 안내 등)
4. 글의 길이나 형식에 제약이 있나요? (SNS 포스트, 블로그, 광고 카피 등)

**개선된 프롬프트 예시**
> "30대 직장인을 타깃으로 한 SaaS 업무 자동화 툴의 SNS(인스타그램) 광고 카피를 작성해줘. 구매 유도가 목적이며, 150자 이내로 간결하게, 공감대를 자극하는 톤으로 작성해줘."
